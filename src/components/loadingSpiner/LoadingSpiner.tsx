import React from "react";
import { SpinerContainer, Spinner } from "./style";

const LoadingSpiner = () => {
  return (
    <SpinerContainer>
      <Spinner />
    </SpinerContainer>
  );
};

export default LoadingSpiner;
